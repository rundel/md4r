% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/modify_node.R
\name{md_node_modify}
\alias{md_node_modify}
\alias{$.md_node}
\alias{$<-.md_node}
\alias{$<-.md_text}
\title{Tools for modifying \code{md_node} objects}
\usage{
\method{$}{md_node}(x, name)

\method{$}{md_node}(x, name) <- value

\method{$}{md_text}(x, name) <- value
}
\arguments{
\item{x}{An object that inherits from \code{md_node}.}

\item{name}{A character string giving the attribute to access, or
\code{"content"} to access the node's children / text content.}

\item{value}{Replacement value for the content or attribute.}
}
\value{
A modified \code{md_node} object.
}
\description{
Overrides the \code{$} operator so the content and attributes of
\code{md_node} objects can be read and updated by name. Use \code{node$content} to
access or replace the children of a block or span node (or the text of a
text node), and \verb{node$<attr>} to read or update an existing node attribute
(e.g. \code{level}, \code{href}, \code{mark}).

Only attributes already present on the node may be replaced, and the new
value is validated against the same rules used by the corresponding
\verb{md_block_*} / \verb{md_span_*} constructor. The \code{class} and \code{names} attributes
cannot be set this way.
}
\examples{
md = md_text_normal("Hello World")
md$content = "Nice to meet you"
md

md = md_block_ul(
  md_block_li(md_text_normal("bullet 1")),
  md_block_li(md_text_normal("bullet 2"))
)
md$mark = "-"
md$content[[1]]$content[[1]]$content = "first"
to_md(md) |> cat(sep = "\n")

}
