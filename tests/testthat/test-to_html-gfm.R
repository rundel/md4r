test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)

test_that(
  label,
  {
    if (test$disabled) {
      testthat::skip("Disabled test(s)")
    }
    sub = (i == skip_tests[["ex"]])
    if (any(sub)) {
      testthat::skip(paste0("gfm #", skip_tests[["ex"]][sub], " - ", skip_tests[["msg"]][sub]))
    }
    expect_identical_html(test$md, "MD_DIALECT_GITHUB", test$html, info = label, url = url)
  }
)
